<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Placeholder fill: MainActivity#refreshConsoleScrollEndButton replaces it
         with the current theme's toolbar colour. -->
    <solid android:color="#FFFFFFFF" />

    <!-- Neutral grey reads on both light and dark consoles, so it survives a
         theme switch untouched. -->
    <stroke
        android:width="1dp"
        android:color="#33808080" />
</shape>
